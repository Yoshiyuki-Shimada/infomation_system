# 更新履歴

## 2026-09-14

### バス発車案内
- 一般路線バスといまざとライナーの残り時間、運行情報、最終便の表示位置と切替を整理。
- 発車前、始発発車遅れ見込み、始発発車未検知、発車情報未検出のステータスを追加。
- 定刻経過後の遅延便を維持しつつ、ページング対象を一般路線バス20分以内、いまざとライナー30分以内に調整。
- 乗換案内を表示中の対象便に連動させ、対象便が消えた場合は案内も消すように変更。
- バス情報の取得間隔を、次便までの時間と遅延・未検知状態に応じて15秒、30秒、60秒へ切り替えるように変更。

### ステータス画像・開発機能
- 一般路線バスの状態画像を、運行日・便ごとに選択したキャラクターで固定し、状態変化時は同じキャラクターの表情を切り替えるように変更。
- 運休画像と通信状態画像を更新し、通信品質に応じて正常、注意、計測中のアイコンを表示。
- 時刻と系統を指定して運休、遅延、始発発車状態を再現できる開発用コマンドを追加。
- 開発PCで開いた index.html にコマンドプロンプトから指示を反映するローカル連携を追加。

### 通信状況表示
- インターネットとローカルネットワークの品質ステータスを比較し、悪い側の状態を通信アイコンへ反映。
- 通信状況ダッシュボードのステータスを左、最終更新日時を右へ配置。

## 2026-08-03

### 時報機能
- 時報停止メニューを、停止時間の選択から再開時刻の選択に変更。
- 現在時刻の次の時報を基準に、10分刻みの再開候補と、30分または00分基準の再開候補を表示するように変更。
- 時報再開時刻選択メニューを中央モーダル表示に変更。
- メニュー上部に「時報再開時刻を選択」を表示。
- 60秒無操作の場合、時報再開時刻選択メニューを自動で閉じるように変更。
- タブレットのタッチ操作でも反応しやすいよう、選択ボタンのタッチ処理と表示間隔を調整。
- 時報停止中は「時報解除」、通常時は「時報停止」と表示するように整理。
- 0:00から5:55までは時報停止ボタンを無効化。
- 夜間時間帯の時報再生を停止。
- 緊急地震速報の音声再生を時報より優先する制御を追加。
- 時報音源の音量を統一。

### いまざとライナー
- 大池橋、田島五丁目の表示幅を揃え、右側に時報停止ボタンを追加。
- いまどこに表示されない便を、大阪シティバスの時刻表データから補完する処理を追加。
- スクロール案内を追加し、停車停留所と乗換案内を表示。
- 予測時刻に応じてスクロール案内を次便へ更新するように変更。
- 行先、経由地、英語表記の一部を修正。
- 最終便、遅延情報、運行情報未取得の表示切替を調整。
- 運行終了表記を中央揃えに変更。

### 田島三丁目バス表示
- 表示中のバスに対して乗換案内のスクロール表示を追加。
- 方面ごとの最下部に案内を表示するように変更。
- 遅延がある場合の案内表示条件を調整。
- 予測時刻または定刻の昇順で表示するように整理。
- 条件に合わない場合は案内画像を表示しないように修正。

### 運行情報・ニュース・天気
- JR西日本公式データから山陽新幹線と一部特急列車の運行情報を取得する処理を追加。
- Yahoo!運行情報で運転再開済みの場合、運転見合わせではなく遅延扱いの表示になるように修正。
- ニュース、天気、運行情報は、取得結果が前回と変わらない場合に不要な更新をしないように変更。
- 週間天気予報は、本日分を取得対象から外すように変更。
- 天気グラフと緊急情報表示が重ならないように表示領域を調整。
- 気象警報、注意報、特別警報などの表示色を整理。

### 地震・緊急情報
- 地震情報、緊急地震速報、津波情報を常時監視する構成を追加。
- 緊急地震速報受信時の音声再生、続報、取消時の再生切替を追加。
- 非常モード時の表示領域、緊急情報表示、下部情報表示を調整。
- 震度情報の表示継続時間と非常モード解除条件を変更。
- 震度3以上の情報を非常モード表示エリアへ表示するように変更。

### 画面表示・端末制御
- 起動時にマウスポインターを非表示にし、マウスを動かした場合のみ一時表示するように変更。
- ピンチイン、ピンチアウトによる拡大縮小を無効化。
- 夜間時間帯にディスプレイを消灯し、朝に復帰する制御を追加。
- アップデート画面表示時にタスクバーが前面に出にくいように調整。
- 更新中画面を2画面に表示する構成へ対応。

### アップデート機能
- 実行PCが更新要求を監視し、メインPCから送信された更新パッケージを適用する仕組みを追加。
- 更新パッケージの署名、ハッシュ確認、バックアップ、再起動処理を追加。
- start.bat、start_displays.ps1、update_agent.ps1 の起動処理とコメント崩れを修正。

### 音源・画像データ
- 時報音源ファイルを更新し、音量を統一。
- GitHubに不足していた画像、音源、制御用スクリプトを追加。